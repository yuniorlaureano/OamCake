@page
@model OamCake.Web.Pages.Admin.Delivery.ListModel
@{
    ViewData["Page"] = "Listado de Deliveries";
    ViewData[nameof(PageTypes)] = PageTypes.DeliveryList;
}

<section class="section dashboard">
    <div class="row">
        <div class="col-12">
            <div class="card recent-sales overflow-auto">

                <div class="filter">
                    <a class="icon" href="#" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></a>
                    <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                        <li class="dropdown-header text-start">
                            <h6>Filter</h6>
                        </li>

                        <li><a class="dropdown-item" href="/admin/delivery/Add">Nuevo</a></li>
                    </ul>
                </div>

                <div class="card-body">
                    <h5 class="card-title">Deliveries</h5>
                    <div class="input-group mb-3 d-flex justify-content-end">
                        <form class="d-flex justify-content-end" method="get" asp-page-handler="OnGet">
                            <input type="text" class="form-control" asp-for="Search" placeholder="Buscar por nombre">
                            <button class="btn btn-outline-secondary" type="submit"><i class="bi bi-search"></i></button>
                        </form>
                    </div>
                    <table class="table table-borderless">
                        <thead>
                            <tr>
                                <th scope="col">Nombre</th>
                                <th scope="col">Teléfono</th>
                                <th scope="col">Dirección</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                           @* @foreach (var delivery in Model.DeliveryTable.Data)
                            {
                                <tr>
                                    <td>@($"{delivery.Employee.Name} {delivery.Employee.LastName}")</td>
                                    <td>@delivery.Employee.Phone</td>
                                    <td>@delivery.Employee.Address</td>
                                    <td style="text-align:right">
                                        <a href="/admin/delivery/add?Id=@delivery.Id" class="btn btn-warning">
                                            <i class='bx bx-edit'></i>
                                        </a>
                                        <partial name="_DeleteForm" model="@delivery.Id" />
                                    </td>
                                </tr>
                            }*@
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan=4">
                                    <nav aria-label="Page navigation">
                                        <ul class="pagination pagination-sm justify-content-end">
                                            @for (var i = 0; i < Model.DeliveryTable.CalcPage(20); i++)
                                            {
                                                <li class="page-item @(i == Model.Pages ? "active": "")" aria-current="page">
                                                    <a class="page-link" href="/admin/delivery/List?pages=@i&Search=@Model.Search">@(i+1)</a>
                                                </li>
                                            }
                                        </ul>
                                    </nav>
                                </td>
                            </tr>
                        </tfoot>
                    </table>

                </div>

            </div>
        </div><!-- End Recent Sales -->
    </div>
</section>